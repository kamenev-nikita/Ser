import Plate from "./tables/Plate";

const FoundationResult = ({ values }) => {
  return (
    <div className="foundationResult">
      <Plate values={values} />
    </div>
  );
};

export default FoundationResult;
